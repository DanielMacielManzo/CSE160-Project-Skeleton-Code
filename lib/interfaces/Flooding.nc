interface Flooding{
    //source addr of the node initiating the flood

    //Monotonically increasing sequence number to uniquely identify the packet

    //A time to live field to avoid the packet looping forever
}
